# Mini MapleStory

Java 기반의 2D 멀티플레이어 온라인 RPG 게임입니다. 메이플스토리에서 영감을 받아 제작되었으며, 외부 게임 엔진이나 라이브러리 없이 순수 Java (Swing, Sockets)로 구현되었습니다.

---

## 목차

- [실행 방법](#실행-방법)
- [조작키](#조작키)
- [주요 기능](#주요-기능)
- [게임 시스템](#게임-시스템)
- [프로젝트 구조](#프로젝트-구조)
- [기술 스택](#기술-스택)
- [네트워크 프로토콜](#네트워크-프로토콜)

---

## 실행 방법

### 1. 컴파일

**Mac/Linux:**
```bash
mkdir -p out && find src -name "*.java" -print0 | xargs -0 javac -d out -cp src
```

**Windows:**
```cmd
mkdir out
dir /s /b src\*.java > sources.txt
javac -d out -cp src @sources.txt
del sources.txt
```

### 2. 서버 실행

```bash
cd out
java server.GameServer
```

서버가 포트 12345에서 클라이언트 연결을 대기합니다.

### 3. 클라이언트 실행

각 클라이언트마다 새 터미널을 열어 실행합니다.

```bash
cd out
java client.GameClient
```

닉네임을 입력하고 게임을 시작하세요!

---

## 조작키

| 키 | 동작 |
|---|---|
| `← →` | 좌우 이동 |
| `Space` | 점프 |
| `Q` | 스킬 1 (범위 400, 데미지 2, 쿨다운 2초) |
| `W` | 스킬 2 (범위 300, 데미지 3, 쿨다운 3초) |
| `E` | 스킬 4 (범위 500, 데미지 4, 쿨다운 4초) |
| `R` | 스킬 3 (범위 600, 데미지 5, 쿨다운 5초) |
| `I` | 인벤토리 열기/닫기 |
| `O` | 장비창 열기/닫기 |
| `S` | 스탯창 열기/닫기 |
| `Z` | 아이템 줍기 (범위 80픽셀) |
| `↑` | 포탈 이동 / NPC 대화 |
| `Enter` | 채팅 |

---

## 주요 기능

### ⚔️ 전투 시스템
- 4가지 스킬 (Q, W, E, R)
- 각 스킬마다 고유한 범위, 데미지, 쿨다운
- 투사체 기반 전투
- 충돌 감지 및 데미지 계산
- 스킬 쿨다운 UI (남은 시간 표시)
- 공격 애니메이션

### 👾 몬스터 시스템
- **Green Slime** (HP: 10) - Hennessis 맵
- **Dragon** (HP: 20) - Boss 맵
- AI 기반 랜덤 이동 (대기, 좌우 이동)
- HP 바 표시
- 몬스터 처치 시 아이템 드롭
- 자동 리스폰 (맵당 최대 개체 수 유지)

### 📊 레벨링 시스템
- 레벨 1~5 (최대 레벨 5)
- 몬스터 처치 시 20 XP 획득
- 동적 XP 요구량 (100 + (레벨-1)*50)
- 레벨업 시 공격력 +3
- 화면 상단에 XP 바 표시

### 🎒 인벤토리 & 아이템
- 24칸 인벤토리 (6x4 그리드)
- 드래그 가능한 인벤토리 UI
- 아이템 더블클릭으로 착용
- Z키로 아이템 습득 (범위 80픽셀)
- 아이템 아이콘 표시

### 💰 화폐 시스템
- 메소 (게임 내 화폐)
- 몬스터 처치 시 50-200 메소 드롭 (100% 확률)
- 장비 아이템 드롭 (10% 확률)
- 메소 획득 시 애니메이션 효과 (위로 떠오르며 페이드아웃)

### 👕 장비 시스템
- 6개 장비 슬롯:
  - 무기 (Weapon)
  - 모자 (Hat)
  - 상의 (Top)
  - 하의 (Bottom)
  - 장갑 (Gloves)
  - 신발 (Shoes)
- 장비 착용 시 캐릭터 외형 변경
- 장비 스탯 보너스 (STR, DEX, INT, LUK, HP, MP, 공격력, 방어력)
- 클릭으로 장비 해제

### 📈 스탯 시스템
- 기본 스탯: STR, DEX, INT, LUK
- 파생 스탯: HP, MP, 공격력, 방어력
- 장비 보너스 적용
- 레벨 보너스 적용
- S키로 스탯 창 열기

### 🗺️ 맵 시스템
3개의 맵으로 구성:

| 맵 이름 | 설명 | 몬스터 |
|--------|------|--------|
| Warrior Room | 시작 맵 | 없음 |
| Hennessis | 일반 사냥터 | Green Slime |
| Boss Map | 보스 맵 | Dragon |

- 포탈을 통한 맵 이동 (↑키)
- 맵별 고유 배경 이미지
- 맵별 다른 땅 높이

### 💬 NPC 시스템
- **주먹펴고 일어서** (Warrior Room)
- NPC 클릭 시 대화 창
- 다중 페이지 대화 지원
- 대화 완료 시 아이템 보상

### 👥 멀티플레이어
- TCP 소켓 기반 실시간 멀티플레이어
- 60 FPS 서버 게임 루프
- 같은 맵의 다른 플레이어 표시
- 플레이어 이름 태그
- 실시간 캐릭터 애니메이션 동기화
- 장비 외형 동기화

### 💬 채팅 시스템
- 전역 채팅
- Enter키로 채팅 입력
- 타임스탬프 표시
- 스크롤 가능한 채팅 히스토리
- 반투명 채팅 창

### 🎬 애니메이션 시스템
- 4가지 애니메이션 상태:
  - Idle (대기)
  - Walk (이동 - 5프레임)
  - Jump (점프 - 2프레임)
  - Attack (공격 - 4프레임)
- 장비에 따른 동적 스프라이트 로딩
- 플레이어별 애니메이터 관리

---

## 게임 시스템

### 데미지 계산
```
최종 데미지 = 스킬 기본 데미지 + 플레이어 공격력 + (레벨 * 3)
```

### XP 요구량
```
레벨업 필요 XP = 100 + (현재레벨 - 1) * 50
```
- 레벨 1→2: 100 XP
- 레벨 2→3: 150 XP
- 레벨 3→4: 200 XP
- 레벨 4→5: 250 XP

### 물리 시스템
- 중력 가속도: 0.5
- 점프 강도: -12.0
- 맵별 땅 높이:
  - Warrior Room: 450
  - Hennessis: 525
  - Boss Map: 525

---

## 프로젝트 구조

```
networkProgramming/
├── src/
│   ├── client/              # 클라이언트
│   │   ├── GameClient.java  # 클라이언트 진입점
│   │   ├── controller/      # 컨트롤러
│   │   │   ├── NetworkHandler.java
│   │   │   ├── PlayerInputHandler.java
│   │   │   └── NpcDialogHandler.java
│   │   ├── util/            # 유틸리티
│   │   │   ├── GameStateParser.java
│   │   │   ├── CharacterAnimator.java
│   │   │   └── SpriteManager.java
│   │   └── view/            # UI
│   │       ├── LoginFrame.java
│   │       ├── LoginPanel.java
│   │       ├── GameFrame.java
│   │       ├── GamePanel.java
│   │       ├── GameRenderer.java
│   │       ├── InventoryPanel.java
│   │       ├── EquipPanel.java
│   │       ├── StatPanel.java
│   │       ├── NpcDialogPanel.java
│   │       └── MesosGainMessage.java
│   │
│   ├── server/              # 서버
│   │   ├── GameServer.java  # 서버 진입점
│   │   ├── core/
│   │   │   ├── GameState.java
│   │   │   └── GameLoop.java
│   │   ├── handler/
│   │   │   ├── ClientHandler.java
│   │   │   └── SkillCreator.java
│   │   ├── map/
│   │   │   ├── GameMap.java
│   │   │   └── MapCreator.java
│   │   └── util/
│   │       ├── GameStateSerializer.java
│   │       └── MessageParser.java
│   │
│   └── common/              # 공유 리소스
│       ├── ImagePath.java
│       ├── dto/
│       │   └── PlayerUpdateDTO.java
│       ├── enums/
│       │   └── Direction.java
│       ├── item/
│       │   └── Item.java
│       ├── map/
│       │   └── Portal.java
│       ├── monster/
│       │   ├── Monster.java
│       │   ├── GreenSlime.java
│       │   └── Dragon.java
│       ├── npc/
│       │   └── NPC.java
│       ├── player/
│       │   └── Player.java
│       ├── skills/
│       │   ├── Skill.java
│       │   ├── Skill1.java
│       │   ├── Skill2.java
│       │   ├── Skill3.java
│       │   └── Skill4.java
│       └── util/
│           ├── ItemSlotMapper.java
│           └── StatCalculator.java
│
├── img/                     # 게임 리소스
│   ├── character/           # 캐릭터 스프라이트
│   ├── clothes/             # 장비 아이템
│   ├── npc/                 # NPC 이미지
│   └── ...
│
└── out/                     # 컴파일 결과물
```

---

## 기술 스택

### 언어 & 프레임워크
- **Java 8+**
- **외부 라이브러리 없음** (순수 Java)

### GUI
- **Java Swing** - 모든 UI 컴포넌트

### 네트워킹
- **TCP Sockets** (port 12345)
- 커스텀 JSON 기반 프로토콜

### 동시성
- `Thread` - 클라이언트 핸들러
- `ExecutorService` (CachedThreadPool)
- `ConcurrentHashMap` - 스레드 안전한 플레이어 맵
- `CopyOnWriteArrayList` - 스레드 안전한 게임 객체 리스트

### 게임 루프
- 서버: 60 FPS 고정 프레임
- 클라이언트: ~60 FPS (16ms Timer)

---

## 네트워크 프로토콜

### 메시지 형식
모든 메시지는 JSON 형식으로 전송됩니다.
```json
{
  "type": "메시지_타입",
  "payload": { /* 데이터 */ }
}
```

### 클라이언트 → 서버

| 메시지 타입 | 설명 | Payload |
|-----------|------|---------|
| `USER_INFO` | 사용자 정보 전송 | `username`, `characterType` |
| `PLAYER_UPDATE` | 플레이어 위치/상태 업데이트 | `x`, `y`, `state`, `direction` |
| `SKILL_USE` | 스킬 사용 | `skillType`, `direction` |
| `USE_PORTAL` | 포탈 사용 | - |
| `CHAT` | 채팅 메시지 | `message` |
| `PICKUP_ITEM` | 아이템 습득 | `itemId` |
| `REQUEST_ITEM_DROP` | NPC 아이템 보상 요청 | - |
| `EQUIP_ITEM` | 아이템 착용 | `itemId`, `slot` |
| `UNEQUIP_ITEM` | 아이템 해제 | `slot` |

### 서버 → 클라이언트

| 메시지 타입 | 설명 | Payload |
|-----------|------|---------|
| `WELCOME` | 플레이어 ID 할당 | `playerId` |
| `GAME_STATE` | 게임 상태 (60 FPS) | 플레이어, 몬스터, 스킬, 아이템, 포탈, NPC, 배경 |
| `CHAT` | 채팅 브로드캐스트 | `message` |
| `ITEM_ADDED` | 인벤토리 아이템 추가 | - |
| `MESOS_UPDATE` | 메소 업데이트 | `mesos`, `gained`, `x`, `y` |

---

## 아키텍처

### 서버 아키텍처
```
GameServer (포트 12345)
    │
    ├─ ClientHandler (각 클라이언트마다 스레드)
    │   ├─ 메시지 수신 및 파싱
    │   └─ GameState 업데이트
    │
    └─ GameLoop (60 FPS)
        ├─ 몬스터 AI 업데이트
        ├─ 스킬 이동 및 충돌 감지
        ├─ 몬스터 리스폰
        ├─ GameState 직렬화
        └─ 모든 클라이언트에 브로드캐스트
```

### 클라이언트 아키텍처
```
GameClient
    │
    ├─ LoginFrame/LoginPanel
    │   └─ 닉네임 입력
    │
    └─ GameFrame/GamePanel
        │
        ├─ NetworkHandler (서버 통신)
        │   ├─ 메시지 송신
        │   └─ 메시지 수신 및 파싱
        │
        ├─ PlayerInputHandler (입력 처리)
        │   ├─ 키보드 이벤트
        │   └─ 마우스 이벤트
        │
        ├─ GameRenderer (렌더링)
        │   ├─ 배경
        │   ├─ 플레이어 (애니메이션)
        │   ├─ 몬스터
        │   ├─ 스킬
        │   ├─ 아이템
        │   ├─ 포탈
        │   └─ NPC
        │
        └─ UI 패널들
            ├─ InventoryPanel
            ├─ EquipPanel
            ├─ StatPanel
            └─ NpcDialogPanel
```

### 데이터 흐름
```
1. 클라이언트 입력 (키보드/마우스)
        ↓
2. PlayerInputHandler가 처리
        ↓
3. NetworkHandler를 통해 서버로 전송
        ↓
4. ClientHandler가 수신 및 GameState 업데이트
        ↓
5. GameLoop에서 게임 로직 실행 (60 FPS)
        ↓
6. GameStateSerializer가 JSON으로 직렬화
        ↓
7. 모든 클라이언트에 브로드캐스트
        ↓
8. GameStateParser가 파싱
        ↓
9. GameRenderer가 화면에 렌더링
```

---

## 주요 클래스 설명

### 서버

| 클래스 | 역할 |
|-------|------|
| `GameServer` | TCP 서버, 클라이언트 연결 수락 |
| `GameState` | 전역 게임 상태 관리 (플레이어, 몬스터, 스킬, 아이템) |
| `GameLoop` | 60 FPS 게임 루프, 상태 브로드캐스트 |
| `ClientHandler` | 개별 클라이언트 연결 및 메시지 처리 |
| `GameStateSerializer` | 게임 상태 JSON 직렬화, 충돌 감지 |
| `MessageParser` | 클라이언트 메시지 파싱 |
| `SkillCreator` | 스킬 객체 생성 팩토리 |
| `GameMap` | 맵 정의 (배경, 몬스터, 포탈, NPC) |
| `MapCreator` | 맵 생성 팩토리 |

### 클라이언트

| 클래스 | 역할 |
|-------|------|
| `GameClient` | 클라이언트 진입점 |
| `NetworkHandler` | 서버 통신 관리 |
| `PlayerInputHandler` | 키보드/마우스 입력 처리 |
| `GameStateParser` | 서버 JSON 파싱 |
| `GameRenderer` | 게임 객체 렌더링 |
| `CharacterAnimator` | 캐릭터 애니메이션 관리 |
| `SpriteManager` | 스프라이트 로딩 및 캐싱 |
| `GamePanel` | 메인 게임 화면 (UI + 렌더링) |
| `InventoryPanel` | 인벤토리 UI |
| `EquipPanel` | 장비 UI |
| `StatPanel` | 스탯 UI |
| `NpcDialogPanel` | NPC 대화 UI |

### 공통 (Common)

| 클래스 | 역할 |
|-------|------|
| `Player` | 플레이어 엔티티 (위치, 스탯, 인벤토리, 장비) |
| `Monster` | 몬스터 기본 클래스 (AI, 이동) |
| `GreenSlime` | 슬라임 몬스터 (HP 10) |
| `Dragon` | 드래곤 몬스터 (HP 20) |
| `Skill` | 스킬 기본 클래스 |
| `Skill1-4` | 4가지 스킬 구현 |
| `Item` | 아이템 (장비, 코인) |
| `NPC` | NPC 데이터 |
| `Portal` | 포탈 (맵 이동) |
| `Direction` | 방향 enum |
| `StatCalculator` | 스탯 계산 (장비 보너스 포함) |
| `ItemSlotMapper` | 아이템→장비슬롯 매핑 |

---

## 개발 환경

- Java 8 이상
- IntelliJ IDEA (권장)
- 외부 라이브러리 불필요

---

## 라이센스

이 프로젝트는 학습 목적으로 제작되었습니다.
